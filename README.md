# Student-Records-Manager
A responsive Student Management System built with React.js that performs CRUD operations (Add, Edit, Delete, View) using in-memory state without a backend. It includes form validation, a simulated loading state, Excel export functionality, and a mobile-friendly dark UI demonstrating modern component-based frontend development.
